import { Box } from "@chakra-ui/react";

const Footer = () => {
  return (
    <Box
      as="footer"
      textAlign="center"
      p={5}
      bg="rgba(0, 0, 0, 0.2)"
      color="black"
    >
      Made with Grackle
    </Box>
  );
};

export default Footer;
